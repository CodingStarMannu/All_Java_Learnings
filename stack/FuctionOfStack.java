package stack;
// stack basically is an abstract datatype.
public class FuctionOfStack {
	
	/* Following functions are in used in stack to perform opertions in the stack.Stack is an abstract function it is a black box function user don't know what is going inside of stack of stack.
	 * push() - used to insert elements in the stack,
	 * top() - top will insert at the top of the stack
	 * pop() pop will remove the topmost element of the stack
	 * size() size will give the size of the stack.
	 * isEmpty()
	 * */
	
	// we can implement stack using Array and LinkedList

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
